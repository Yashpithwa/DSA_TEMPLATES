👉 Use for:

Upper Bound

Last Occurrence

Floor

int low = 0, high = n - 1;
int ans = -1;

while (low <= high) {
    int mid = low + (high - low) / 2;

    if (condition(mid)) {
        ans = mid;
        low = mid + 1;    // move RIGHT
    } else {
        high = mid - 1;
    }
}

return ans;
